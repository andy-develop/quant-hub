__pycache__/
*.pyc
.pytest_cache/
web/dist/
*.log

# ★ state/payload 与 state/publish 入 git（方案 §3/§4.5）：
#   payload = carry-forward 的"上次成功信封"，publish = HSK 幂等指纹与资源留档。
#   此前被忽略会让"任一域挂掉用上版顶上"失效，故取消忽略。
# 仅忽略本地数据版本与临时件：
state/**/.local.json
*.tmp

# 本地数据仓（数据仓/代码仓分离，方案 §2）：不进代码仓
data/
domains/*/data/

# 遗留本地中间件（无代码消费，勿误提交）
state/data/calendar.json
state/data/status.json
